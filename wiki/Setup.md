# Protocol Buffers (.proto) binary streams

```bash
npm install @grpc/grpc-js @grpc/proto-loader
```

```
src/
├── workouts.proto      <-- The strict language-neutral type contract
├── service.ts          <-- The "Microservice" (Server holding the data)
└── client.ts           <-- The "App Backend" (Client asking for the data)
```
