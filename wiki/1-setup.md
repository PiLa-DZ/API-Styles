```bash
npm install graphql graphql-yoga
# NOTE:
# We are using graphql-yoga because it is an ultra-modern,
# lightweight, high-performance GraphQL server engine
# that sets up in seconds without any boilerplate).
```
